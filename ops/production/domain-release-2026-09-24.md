# Публичный домен Artel CRM — 24 сентября 2026

Основной адрес: https://artel-crm.online/.

## Размещение

- Приложение продолжает работать на VPS `130.49.151.239`, проект Compose `artel-crm`, контейнер `artel-crm-crm-1`.
- Образ: `artel-crm:20260924-domain2`; коммит кода `1bffe0a`. Образ основан на прежнем `artel-crm:20260924-release1`, заменён только серверный bundle.
- Исправлен отказ 403 при обычном переходе по внешней ссылке. Исключение допускает только загрузку публичной страницы верхнего уровня методом GET; проверки Host/Origin, API, закрытых путей и остальных запросов сохранены.
- `ARTEL_PUBLIC_ORIGIN=https://artel-crm.online`; остальные значения `runtime.env` не изменялись. Изменение выполнено под `.runtime-env.lock`, права файла — `0600`.
- Приложение по-прежнему доступно на хосте только через `127.0.0.1:18083`. Добавлен отдельный `/etc/caddy/sites-enabled/artel-crm.caddy`; конфигурация проверена и применена через reload.

## DNS и TLS

- В зоне ISPmanager хостинга `u3658979` A-записи `artel-crm.online` и `www.artel-crm.online` направлены на `130.49.151.239`, TTL 300.
- У этих двух имён удалены прежние AAAA-записи `2a00:f940:2:2:1:1:0:125`. Прочие DNS-записи не менялись.
- Авторитетная зона обновилась до serial `2026092405`; 26 из 28 серверов ответили этой версией, два запроса завершились тайм-аутом.
- Для основного имени и `www` выпущены действительные сертификаты Let's Encrypt, срок до 23 декабря 2026; дальнейшее обновление выполняет Caddy. HTTP перенаправляется на HTTPS, HTTPS `www` — на основной домен.
- Во время подключения рекурсивные DNS возвращали разные версии зоны. У провайдеров могут временно оставаться старые записи с прежним TTL 3600.
- Финальная проверка с обычным DNS, без принудительного IP: основной HTTPS — 200, основной HTTP — 308, HTTPS `www` — 301 на основной домен; проверка TLS успешна. Оба имени разрешились в `130.49.151.239`.
- Приложение не размещено на Host-0: этот хостинг сейчас обслуживает DNS-зону. Перед отключением услуги необходимо отдельно перенести DNS.

## Проверки

- 6 production runtime тестов, TypeScript, ESLint изменённых файлов и сборка сервера прошли.
- 42 проверки публичного HTTPS/API: вход директора и менеджера, Secure/HttpOnly/SameSite cookies, разграничение доступа, закрытые пути, внешняя ссылка на страницу разрешена, такой же запрос к API запрещён.
- Через авторизованный API доступны 100 отгрузок, 676 компаний, 207 операций Сбера и 8 ранее сохранённых операций Т-Банка.
- Реальный вход и выход проверены во встроенном браузере на публичном домене, без туннеля и без отключения проверки TLS. Загружена таблица отгрузок; desktop 1440×900 и mobile 390×844 не имеют горизонтального переполнения страницы. Мобильное меню работает, ошибок и предупреждений JavaScript нет.
- Сбер НК АРТЭЛЬ штатно обновился в 16:08:01 МСК: 207 операций, ошибки и незавершённое задание отсутствуют. Отдельная банковская синхронизация для проверки не запускалась. Т-Банк остаётся без активного токена.
- После удаления временных пользователей осталось ровно два реальных пользователя; SHA-256 их полных записей совпадает со значением до подключения домена. Временные сессии, локальные учётные данные и вспомогательный сервер удалены/закрыты.
- Исходные Caddy-файлы, PID Caddy и ID/StartedAt семи контейнеров FuelLead/Руспласта совпали с исходными показателями; HTTP-коды — 200/200/204.

## Резервные копии и откат

Свежие архивы создавались штатным `artel-crm-backup.service` до изменений и после проверки. Отдельные копии прежних `runtime.env`, `.env` с тегом образа, `release.json` и обезличенные сводки находятся в `/opt/artel-crm/backups/domain-20260924/`.

Для отката кода вернуть только `ARTEL_IMAGE=artel-crm:20260924-release1` и пересоздать службу `crm`. Актуальные данные, банковские токены, origin и DNS сохранять; не восстанавливать старое хранилище из архива.

Закрытые локальные результаты: `~/Library/Application Support/Artel-CRM/deployment/2026-09-24-reg-ru/domain-2026-09-24/` — `verification-public.json`, `verification-browser-public.json`, `other-projects-before.txt`, `other-projects-after.txt`, `release.json`.

Изменения выполнены в выделенном worktree `f8fc`; исходная Desktop-папка, старое Vercel-размещение и отдельное подключение Сбера ООО АРТЭЛЬ не менялись. Push в GitHub не выполнялся.
