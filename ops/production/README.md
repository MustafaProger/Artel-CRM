# Размещение Artel CRM на REG.RU

Проверено 24 сентября 2026 года. Сервер: `130.49.151.239`.

## Текущее состояние

- Отдельный Compose-проект `artel-crm`, контейнер `artel-crm-crm-1`.
- Образ `artel-crm:20260924-domain2`; коммит серверного кода `1bffe0a` (базовый production runtime и TLS — `9d61796`).
- Приложение слушает только `127.0.0.1:18083` хоста; внутри контейнера порт `8080`.
- Публичный origin: `https://artel-crm.online`. DNS, действительный HTTPS, вход, данные и desktop/mobile UI проверены. Итоги: [подключение домена 24 сентября](domain-release-2026-09-24.md).
- Node 22, непривилегированный пользователь, read-only образ, отдельные данные, ограничения CPU/памяти/журнала.
- `restart: unless-stopped`; почасовой `artel-crm-backup.timer` включён.
- Сбер **НК АРТЭЛЬ**: реальная проверка выписки и обновление токенов прошли; автоматическое обновление работает. При проверке после переноса стало 207 сохранённых операций.
- Т-Банк: доверенная цепочка исправлена; реальный запрос со старым токеном получил HTTP 403. Пользователь попросил временно пропустить новый токен. Токен в активной конфигурации пустой, автоматические запросы пропускаются; 8 ранее загруженных операций сохранены. Прежний токен не отозван и остаётся в закрытой резервной конфигурации.
- Исходящий IPv4 именно контейнера проверен через два HTTPS-сервиса: `130.49.151.239`. Интерфейс macOS `en0` в Linux не перенесён.
- Отдельная параллельная локальная задача подключения Сбера **ООО АРТЭЛЬ** не включена в этот релиз и не изменялась.

## Размещение файлов

| Путь на сервере | Назначение |
| --- | --- |
| `/opt/artel-crm/compose.yaml` | Только служба CRM |
| `/opt/artel-crm/.env` | Тег образа |
| `/opt/artel-crm/secrets/runtime.env` | Серверные секреты, `0600`; Compose `env_file.format: raw` |
| `/opt/artel-crm/data/operations/operations.json` | Атомарное изменяемое хранилище, `0600` |
| `/opt/artel-crm/data/snapshot` | Проверенный исходный снимок, read-only mount |
| `/opt/artel-crm/backups` | Закрытые архивы конфигурации и данных |
| `/opt/artel-crm/releases` | Артефакты сборки и инструкции |
| `/opt/artel-crm/ops` | Только обслуживающие скрипты этой CRM |

Секреты не входят в Git, браузерную сборку или Docker image. Не выводить `docker compose config`, `docker inspect` без ограниченного формата, `runtime.env` и полное хранилище в общий журнал. Для синтаксиса Compose использовать `docker compose config --quiet`.

## Перенос данных

Локальные и старые публичные данные Vercel были прочитаны и скопированы до изменений. Их нельзя было заменять друг другом.

Сохранены 100 действующих отгрузок, два tombstone, 676 компаний, production-исправление 65 компаний, дополнительные справочники и удаления, архивная работа со вложением, данные раздела «Китай», две учётные записи, выписки обоих банков. По выбору пользователя пароль и настройки директора взяты из локальной CRM; облачный ID оставлен для целостности исторических ссылок. Старые сессии и push-подписки в новой копии очищены.

Проверены все 316 ссылок, контрольные суммы и декодирование. Временные проверочные пользователи удалены; реальные пароли не менялись. До переноса повторно подтверждена неизменность cloud-снимка. Старый сайт Vercel и исходная Desktop-папка не изменялись.

Закрытый локальный архив переноса:

`/Users/mustafa/Library/Application Support/Artel-CRM/deployment/2026-09-24-reg-ru/`

В нём находятся исходный архив, облачный снимок, манифест объединения, результаты проверок и копия серверного backup. Эти файлы содержат закрытые данные и не предназначены для публикации.

## Подключение домена

1. Получить от пользователя отдельный домен или поддомен и направить его A-запись на `130.49.151.239`. Не использовать имена и HTTP-маршрут FuelLead/Руспласт.
2. Проверить DNS снаружи. Сохранить резервную копию текущей конфигурации CRM.
3. Под эксклюзивным `/opt/artel-crm/secrets/.runtime-env.lock` заменить `ARTEL_PUBLIC_ORIGIN` на точный HTTPS origin нового адреса. Формат файла — необработанные `KEY=value`, без shell expansion и без внешних кавычек.
4. Пересоздать только CRM: `docker compose --project-directory /opt/artel-crm up -d --no-build --pull never crm`.
5. Создать только новый `/etc/caddy/sites-enabled/artel-crm.caddy` по `site.caddy.template`. Существующий Caddyfile уже импортирует `sites-enabled/*.caddy`.
6. Проверить `caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile`. При успехе применить **reload**, не restart: `systemctl reload caddy`. Общие virtual hosts не редактировать.
7. Проверить публичные DNS, HTTPS-сертификат, перенаправление HTTP, реальный вход пользователя, API, desktop/mobile UI. Повторно сравнить контрольные суммы старых Caddy-файлов, PID Caddy, ID/StartedAt старых контейнеров и HTTP 200/200/204 FuelLead/Руспласт/CMS.

Порты общего firewall не менялись. `18083` не следует открывать наружу.

## Банки и переход на сервер

`ARTEL_BANK_REQUESTS_ENABLED=false` полностью блокирует новые банковские запросы production runtime. Для расписания нужны оба флага `ARTEL_BANK_REQUESTS_ENABLED=true` и `ARTEL_BANK_SYNC_ENABLED=true`. Сохранённые данные остаются доступны.

Сбер использует актуальную зашифрованную пару из `operations.json` и прежний `ARTEL_BANK_ENCRYPTION_KEY`. Сервер уже обновил эту пару. **Не запускать старую локальную синхронизацию НК АРТЭЛЬ с прежним снимком токенов.** Во время перехода соответствующий локальный процесс не работал; отдельный локальный процесс Сбер ООО АРТЭЛЬ использует другое подключение.

Т-Банк отложен по просьбе пользователя. Когда он будет готов:

1. Ещё раз проверить исходящий IP из контейнера; выпустить токен с этим IP и правами чтения счетов/операций. Старый доступ не отзывать до успешной проверки нового.
2. Сохранить новый токен через скрытый ввод: `ssh -t root@130.49.151.239 'python3 /opt/artel-crm/ops/set-tbank-token.py'`. Вариант `--stdin` предназначен только для закрытого канала передачи, без значений в argv или журнале.
3. Пересоздать только контейнер CRM и выполнить реальное чтение выписки. Проверить завершение полного периода, сохранённые операции и отсутствие дублей. Если старая задача остановилась после пяти ошибок, явно продолжить тот же период после успешного теста доступа.
4. Проверить автоматическую следующую загрузку. Не считать наличие токена доказательством подключения.

Скрипт установки сохраняет прежнюю конфигурацию `0600` и атомарно заменяет только значение токена. Он не включает расписание и не выполняет платежи. Локальная форма приёма токена закрыта после просьбы отложить этот шаг.

Только подключение Т-Банка получает `ARTEL_BANK_TBANK_NK_TLS_CA_BASE64`. Системное доверие, другие проекты и проверка TLS не менялись. Публичные CA получены по ссылкам из [инструкции Т-Банка](https://developer.tbank.ru/docs/tls-settings); подробности в `docs/banking.md`.

## Резервные копии и откат

`artel-crm-backup.timer` запускает backup ежечасно. Проверяется SHA-256 JSON envelope с тем же JavaScript-сериализатором и содержимое полученного архива. Сохраняются 72 последние часовые копии и по одной в день за 30 дней; очистка касается только точного шаблона архивов этой CRM. При свободном месте менее 1 GiB backup отказывается начинать упаковку. Начальные архивы миграции сохраняются отдельно.

Ручной backup: `systemctl start artel-crm-backup.service`; проверка: `systemctl show artel-crm-backup.service -p Result -p ExecMainStatus`. Восстановление backup проверено в отдельном контейнере **без сети**, без банковских секретов в окружении и без публикации портов; восстановленная CRM загрузила данные и ответила 200.

Для отката кода оставить актуальные данные и секреты на месте, указать проверенный предыдущий образ в `/opt/artel-crm/.env` и пересоздать только `crm`. Для полного отключения нового размещения использовать `docker compose --project-directory /opt/artel-crm stop crm`; при необходимости отключить только `artel-crm-backup.timer`. Другие проекты не затрагивать.

Перед откатом данных выключить запросы к банкам, остановить только CRM и сохранить последнюю копию всего хранилища с конфигурацией. **Нельзя возвращать старые refresh tokens из архива:** сначала сохранить актуальный `sber.encryptedTokens` и неизменный ключ шифрования, восстановить бизнес-данные в отдельной папке и проверить их декодером; банковскую часть переносить из самого свежего рабочего состояния. Возврат к локальному приложению требует такого же переноса актуальных данных и отключения серверной синхронизации. Старый Vercel-снимок не является полным банковским откатом.

## Проверки этого размещения

- 213 существующих API-тестов, 5 новых production-тестов, TypeScript, ESLint и production build.
- Проверка браузерной сборки: реальные секреты в 14 публичных файлах не обнаружены.
- 20 проверок реального серверного API: вход, Secure/HttpOnly cookies, данные, права менеджера, Host/Origin, закрытые пути.
- После restart сохранены сессия и бизнес-данные; 4 браузерные проверки реального backend через SSH, desktop/mobile, без ошибок JavaScript.
- Linux-тест entrypoint: запрет второго writer, освобождение flock после SIGKILL и безопасное удаление устаревшего transaction marker.
- Проверены работающий backup timer и восстановление архива без сети.
- Реальное чтение Сбера и последующая полная автоматическая загрузка; Т-Банк дошёл до ответа банка 403 при корректном TLS.
- Все 13 записей базового состояния существующих проектов совпали после работ: контейнеры не перезапускались, Caddy PID и прежние конфигурации не изменились.
- 24 сентября подключён `https://artel-crm.online`: 42 проверки публичного HTTPS/API и реальный браузерный вход; desktop 1440×900 и mobile 390×844, ошибок JavaScript нет. Оба реальных пользователя сохранены без изменений, временные проверочные учётки и их сессии удалены.
