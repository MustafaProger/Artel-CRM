# PROJECT_STATUS — передача проекта на 14 сентября 2026

> Дополнение по реальному подключению Sber API: см. раздел 9 в конце документа. Статус банковского доступа оценивается отдельно от публикации CRM.

## 1. Состояние этапа и текущая цель

Текущий этап завершён: рабочая CRM с авторизацией, ролями, отгрузками, справочниками, задачами, учётом Китая, АЗС и Web Push находится в `main` и опубликована на `https://artel-crm.vercel.app`.

- Ветка: `main`.
- Текущий commit: `f5cae902681e638b8e12fac48a1803d3c02293a5` (`Redesign sign-in screen and refine sidebar controls and transitions`).
- До обновления этого документа рабочее дерево было чистым, `HEAD`, `origin/main` и `origin/HEAD` указывали на один commit.
- Production отдаёт те же JS/CSS-файлы, что свежая локальная сборка. Неавторизованный API возвращает `401`, а `/.env` и исходные JSON не раздаются (`404`). `/api/auth/session` подтверждает, что первоначальная учётная запись уже создана (`needsSetup: false`) и setup защищён серверным токеном.

Ближайшая цель следующего этапа — выполнить авторизованную приёмку production с реальной учётной записью и закрыть внешние интеграции/незаполненные разделы, не меняя существующую бизнес-логику и рабочие данные без отдельного решения владельца.

## 2. Что завершено

### Доступ и безопасность

- Реализованы серверные сессии, вход/выход, первичная настройка директора, смена пароля и роли `director`, `admin`, `manager`.
- Область данных менеджера ограничивается на сервере; управляющие действия скрыты в интерфейсе и дополнительно запрещены API.
- Мутации защищены проверкой Origin, версиями записей и конфликтами конкурентного изменения.
- Экран входа приведён к стилю FuelLead; сохранены setup, login, loading, error и retry-состояния, клавиатурная навигация и reduced motion.

### Рабочие разделы

- «Работа»: задачи, заметки и работа с компаниями, исполнители, комментарии, приватные файлы, напоминания, архив/возврат одним действием и удаление с проверкой прав и версии.
- «Отгрузки»: бензовозы и отдельный режим АЗС, создание/редактирование/удаление машины с несколькими клиентами, распределение тоннажа и затрат, оплаты, фильтры, сортировка, CSV и виртуализация.
- Формула новых бензовозных отгрузок следует шаблону формы оплаты; исторические исходные значения не перезаписываются. АЗС использует отдельные правила и не принимает F2.
- Ширины столбцов меняются как в Excel на широком десктопе, сохраняются отдельно по виду таблицы и сбрасываются мышью или клавиатурой; мобильные размеры от них не зависят.
- «Справочники»: компании, менеджеры, товары, формы оплаты, водители, автомобили и адреса; удаление учитывает зависимости и tombstone-записи. Архивирование клиентов/поставщиков сохраняет исторические ID и связи.
- «Китай»: несколько заправок за день, независимые платежи и точный баланс за всё время `поступления − заправки`; на узких экранах таблица прокручивается внутри контейнера.
- PWA-иконки, manifest и service worker подключены. Web Push умеет подписывать устройство, отправлять тест, доставлять напоминания и исключать повторы; GitHub Actions настроен на dispatch каждые 5 минут через OIDC.

### Хранение и публикация

- Исходный снимок `data/local-xlsx-final/` считается неизменяемым. Все рабочие изменения хранятся отдельно в envelope `operations.json` с checksum и `revision`.
- Локальная запись атомарна и защищена lock-файлом. Production использует приватный Vercel Blob `artel/operations.json`, сильный ETag и `If-Match`; при конфликте возвращается `409`, повреждённое или отсутствующее хранилище не заменяется пустым.
- Очистка операций и справочников отделена от старта приложения, требует явного запуска, создаёт и перечитывает резервную копию. Флаг `sourceOperationsCleared` не позволяет импортированным операциям появиться снова.
- Автоматическая публикация идёт из GitHub `MustafaProger/Artel-CRM`, ветка `main`, в проект Vercel `artel-crm`.

## 3. Ключевые технические решения

| Область | Решение, которое нужно сохранять |
| --- | --- |
| Архитектура | React/Vite в `web/`; единый Node API создаётся в `server/local-api.ts`; Vercel-адаптер — `server/cloud-handler.ts`. |
| Данные | Не править исходный XLSX/JSON при работе CRM. Изменения, удаления, аккаунты, задачи, Китай и push живут в отдельном operations-store. |
| Конкурентность | На клиентских сущностях используются версии; локально — сериализация и lock, в Blob — сильный ETag/`If-Match`. Не обходить конфликт перезаписью. |
| Авторизация | Проверки ролей и области данных обязательны на сервере; скрытие кнопок не считается защитой. Пароли хешируются, сессии хранятся в HttpOnly cookie. |
| Расчёты | Денежные и китайские суммы обрабатываются точной десятичной арифметикой. Исторические результаты Excel сохраняются, новые правила применяются только там, где это явно предусмотрено. |
| QA | Браузерные CRUD-проверки запускаются на временном `ARTEL_STORE_DIR`; production и рабочий локальный store не должны участвовать в тестовых мутациях. |
| Push | Разрешение запрашивается только действием пользователя. Постоянный секрет GitHub не нужен: планировщик подписывает вызов OIDC-токеном, который проверяется сервером. |

Локальный запуск: `npm run dev`, адрес `http://127.0.0.1:5173`. Закрытые настройки задаются только через локальное/production-окружение; реальные значения не должны попадать в Git, отчёты или логи.

## 4. Основные изменённые файлы

| Файлы | Назначение |
| --- | --- |
| `web/src/App.tsx`, `web/src/apple-theme.css` | Навигация, адаптивная оболочка, пустые/рабочие разделы, общая тема. |
| `web/src/AuthGate.tsx`, `web/src/auth.css`, `server/auth.ts`, `server/auth-scope.ts` | Вход/setup, сессии, роли и серверная область доступа. |
| `web/src/WorkPage.tsx`, `server/work-operations.ts`, `web/src/PushSettings.tsx`, `server/push.ts` | Задачи, архив/удаление, комментарии/файлы, подписки и напоминания. |
| `web/src/ShipmentsPage.tsx`, `web/src/ShipmentTripEditor.tsx`, `web/src/ShipmentAzsEditor.tsx`, `web/src/ShipmentColumnResize.tsx` | Таблицы и редакторы бензовозов/АЗС, несколько клиентов и изменение ширин. |
| `server/shipment-operations.ts`, `server/shipment-trips.ts`, `web/src/shipment-calculations.ts`, `web/src/shipment-templates.ts` | Серверные операции, атомарные рейсы, расчёты и наборы колонок. |
| `web/src/DirectoriesPage.tsx`, `server/directory-editing.ts`, `server/directory-deletion.ts`, `server/directory-cleanup.ts` | Редактирование, зависимости, удаление и архивирование справочников. |
| `web/src/ChinaPage.tsx`, `web/src/china-calculations.ts`, `server/china-operations.ts` | Учёт и баланс Китая. |
| `server/operations-store.ts`, `server/blob-operations-store.ts`, `server/local-api.ts`, `server/cloud-handler.ts` | Валидация и сохранение общего состояния, API, локальное и облачное хранилище. |
| `.github/workflows/reminders.yml`, `server/push-cron-auth.ts`, `web/public/sw.js` | Плановый dispatch, проверка GitHub OIDC и показ уведомлений. |
| `scripts/verify_*.mjs`, `tests/api*.test.ts` | Изолированные браузерные и API-регрессии. |
| `docs/ui-refinements-2026-09-11.md`, `docs/web-notifications.md`, `docs/implementation-2026-09-10.md` | Подробности последнего UI-этапа, push и внедрения. |

## 5. Проверки на момент передачи

Свежий прогон 14 сентября 2026:

- `npm run typecheck` — успешно.
- `npm run lint` — успешно.
- `npm run build` — успешно; Vite собрал 1 609 модулей.
- `npm run test:api` — 93/93 теста успешно.
- Python export/verifier tests — 12/12 успешно в одноразовом virtualenv с `requirements.txt`.
- `npm run test:auth-ui` — успешно: setup, вход/выход, роли, запреты менеджера, мобильное меню, отсутствие JS-ошибок и горизонтального overflow на 1440/768/390/320 px.
- `npm run test:design-ui` — успешно: 9 страниц на 1440/1024/768/390/320 px, Китай, меню/фокус, редакторы, reduced motion; Axe не нашёл нарушений в проверенных состояниях.
- `node scripts/verify_ui_refinements.mjs` — успешно: ширины 320–1920 px, изменение колонок, отдельные настройки АЗС, справочники, иконки и Chromium/WebKit-профили.
- `git diff --check` — успешно.

Production smoke-check без входа:

- `/` и `/api/auth/session` — `200`;
- защищённый `/api/snapshot` — `401` без сессии;
- `/.env` и `/data/local-xlsx-final/shipments.json` — `404`;
- ссылки на собранные JS/CSS в production совпадают со свежей локальной сборкой.

Авторизованная проверка реальных production-данных и мутаций в этом handoff не выполнялась, чтобы не использовать учётные данные и не менять рабочее состояние.

## 6. Известные проблемы и ограничения

- «Операторская» пока только сообщает, что исходный Excel и правила не предоставлены. Реализацию нельзя начинать с предположений о колонках или формулах.
- «Обзор» и «Склад» намеренно пустые после согласованной очистки; в «ЗП» есть только вкладки водителей/менеджеров без расчётов.
- Актуальная Google-таблица не предоставлена и не сверена. Текущий immutable snapshot основан на `БХЧ Артэль 2026 (5).xlsx`; ошибки и неполные формулы самого источника перечислены в `docs/source-audit.md`.
- Живой запрос к Чекко ранее завершался `502`/таймаутом. Тестовый контракт покрыт, ручное заполнение доступно, но реальная интеграция из текущей сети/production требует повторной проверки.
- Реальная push-доставка на физическое устройство и фактическая регулярность GitHub schedule в этом handoff не проверялись. Для iPhone приложение нужно добавить на экран «Домой»; GitHub schedule может задерживаться и отключается в публичном репозитории после длительной неактивности.
- `README.md` и начало `docs/vercel-deployment.md` содержат устаревшие формулировки о публичном API, локально не опубликованных функциях и старом количестве тестов. Ориентироваться на этот файл и текущий код; документацию нужно синхронизировать следующей отдельной правкой.
- `scripts/verify_vercel.mjs` рассчитан на прежний публичный API и полное равенство local/production. После включения авторизации и начала независимой работы облачного store этот сценарий больше не является корректной production-проверкой.

## 7. Опробованные, но не сработавшие подходы

- Чекко не стал доступен после принудительного IPv4/TLS 1.2; по одному сетевому таймауту нельзя делать вывод о неверном ключе или исчерпанном лимите.
- Первый ad-hoc Axe-прогон через `browser.newPage()` завершился ошибкой `Please use browser.newContext()`. Для Axe/Playwright нужен отдельный `browser.newContext()`; исправленный прогон прошёл.
- Desktop headless WebKit зависал при активном service worker. В этом тестовом профиле service worker блокируется; push проверяется отдельным сценарием и на мобильных профилях.
- Сравнение полного рабочего store до/после браузерного QA давало ложное изменение из-за фонового push heartbeat/revision. Актуальные сценарии используют временное хранилище и не смешивают тестовые записи с рабочими.
- Системный `/usr/bin/python3` (3.9.6) не запустил export-тесты из-за отсутствующего `openpyxl`. Одноразовый virtualenv с зависимостями из `requirements.txt` решил проблему; менять код тестов не потребовалось.

## 8. Следующие шаги

1. Синхронизировать `README.md`, `docs/vercel-deployment.md` и `scripts/verify_vercel.mjs` с текущей обязательной авторизацией и независимым production store.
2. С владельцем выполнить безопасную авторизованную production-приёмку: вход, роли, чтение ключевых разделов, состав справочников и состояние данных. Для проверки записи использовать заранее согласованную тестовую сущность с последующим удалением либо отдельное окружение.
3. Проверить без раскрытия значений наличие production-настроек для Чекко и Web Push, состояние последнего workflow `Send CRM reminders`, затем подтвердить тестовое уведомление на реальном компьютере и iPhone/Android.
4. Получить Excel для «Операторской», актуальную Google-таблицу и утверждённые правила для «Обзора», «Склада» и «ЗП». До этого не придумывать расчёты и структуру данных.
5. При новом источнике сначала создать отдельный snapshot, сверить контрольные суммы/формулы, подготовить миграцию ссылок и резервную копию Blob; не заменять текущий источник обычным обновлением файла.
6. После каждого следующего изменения запускать typecheck, lint, API, релевантные browser-suite и production build на временном store; перед публикацией проверять diff на секреты и после push подтверждать remote SHA, Vercel build и живое поведение отдельно.

## 9. Реальное подключение Sber API — 14.09.2026, продолжается

Исходный банковский код находится в `2fa94a4` и был опубликован до начала подключения. Синтетические тесты не означают, что банк подключён.

### Выполнено в СберБизнесе

- В существующей вкладке Chrome определена **ООО «НК АРТЭЛЬ»**; целевое подключение CRM — `sber-nk-artel` («СберБизнес — НК АРТЕЛЬ»). В банке показан рублёвый расчётный счёт `••••5571`. Полный номер ещё не перенесён на сервер. Вторую организацию и Т-Банк не настраивали.
- Пройдено **Все продукты и услуги → Сбербанк API → Sber API → Подключить → Компаниям → Продолжить**. Тариф в карточке — **Бесплатно**. Заявление требует контактные email/телефон и ответственного за хранение ключевой информации. Произвольные лица и контакты не подставлялись.
- После самостоятельного прохождения заявления пользователем сервис появился в ЛК. Агент нажал **Активировать** на вкладке **Промышленный сервис**; банк подтвердил **Активирован**. Это активация сервиса банка, а не подтверждение доступа CRM.
- Сервис **Sber API: 5050140563_Company**, `client_id=88091`, относится к ООО «НК АРТЭЛЬ», ИНН `5050140563`. Карточка: `https://sbi.sberbank.ru:9443/ic/ufs/host/index.html#/sbbapi/service-information/88091`. Client ID не является секретом.
- Пройдено **Сертификаты шифрования → Сгенерировать сертификат**. Пользователь выполнил шаг с паролем и сообщил «все сделал». Последний скриншот подтверждает **«Сертификат сгенерирован»** и срок **14.09.2026–14.09.2027**; в окне есть ссылка скачивания. Завершение скачивания, фактическое расположение `.p12`, его целостность, пароль и установка на сервер ещё не проверены. Скриншот «Связки ключей» сам по себе не подтверждает импорт именно этого сертификата. Продолжать с уже выпущенным контейнером; повторно выпускать сертификат без необходимости не нужно. Секреты через чат не передаются.
- Доступный scope сервиса широкий, включая `PAY_DOC_RU`, `PAY_DOC_CUR`, `PAYROLL`. Состав показан пользователю. Для CRM согласована исключительно читающая интеграция. Планируемый OAuth scope: `openid GET_STATEMENT_ACCOUNT inn orgFullName accounts`. Токены из ЛК с неподтверждённым объёмом прав не выпускались; авторизация с ограниченным scope ещё не выполнена.
- Реальные движения видны в интерфейсе банка, но **0 операций получено через API в CRM**. Сверка API с выпиской не выполнена.

### Сервер и сохранность

- Фактический production: `https://artel-crm.vercel.app`, Vercel Node Function в регионе `iad1`. В проверенном production-окружении банковские переменные отсутствовали.
- Проверенная резервная копия production: `data/backups/sber-2026-09-14/production-before.json` (вне Git, 0600), revision 109, SHA-256 `ea08d4fc3571a965f9f40bc9b81a5045a8fb9c87721e476212d274d9b542c327`. Содержимое не выводилось.
- Исправлен HTTP 202: формирование выписки вызывает повтор минимум через 60 секунд, завершённые данные и курсор сохраняются. Поддерживается `.p12`: `_TLS_PFX_PATH` / `_TLS_PFX_BASE64` и `_TLS_PASSPHRASE` как альтернатива PEM cert/key.
- Добавлена защищённая диагностика сети: `GET /api/banking/network-check?connection=sber-nk-artel` для директора/администратора; через действующую авторизацию планировщика — `GET /api/banking/dispatch?check=sber-network`. Фиксированный адрес `fintech.sberbank.ru:9443`, без токена/счёта, изменения хранилища и вывода ответа банка. Проверка TLS включена. Доступность HTTP сама по себе не подтверждает доступ к выписке.
- Локально без клиентского сертификата/цепочки: TCP 9443 доступен, TLS отвергнут с `SELF_SIGNED_CERT_IN_CHAIN`. Нужна официальная доверенная цепочка; отключать проверку TLS нельзя. Результат из Vercel записывается отдельно после проверки.

### Процедура для второй организации и оставшиеся шаги

1. Выбрать организацию в СберБизнесе и проверить ИНН/наш счёт. Повторить меню из раздела выше; набор **Компаниям** подключается отдельно для каждой организации. «Холдингам» — другой договорный сценарий и здесь не использовался.
2. Заполнить контакты/ответственного, создать заявление, проверить условия, подписать СМС/аппаратным токеном самостоятельно. После появления сервиса открыть **Промышленный сервис → Активировать**.
3. Выпустить промышленный TLS-контейнер. У каждого client_id свой сертификат, срок по документации — 12 месяцев. При выпуске через CSR указан RSA 2048. `.p12` содержит клиентский ключ; цепочка Сбера/Минцифры нужна для проверки банковского сервера. Это не ЭП для платежей.
4. Установить доступы только в защищённые серверные настройки. Для НК АРТЭЛЬ префикс `ARTEL_BANK_SBER_NK`, для АРТЕЛЬ — `ARTEL_BANK_SBER_ARTEL`. Суффиксы: `_ACCOUNTS` (JSON номеров/валют), `_CLIENT_ID`, `_CLIENT_SECRET`, первичный `_REFRESH_TOKEN`, `_TLS_PFX_BASE64` + `_TLS_PASSPHRASE` **либо** `_TLS_CERT_BASE64` + `_TLS_KEY_BASE64`; `_TLS_CA_BASE64`. Варианты `_PATH` предназначены для файлового сервера. Доступы первой организации нельзя копировать во второе подключение.
5. Создать `ARTEL_BANK_ENCRYPTION_KEY`: случайные 32 байта в hex. Ротируемые токены шифруются AES-256-GCM с привязкой к connection ID. Ключи и ответы OAuth не должны попадать в браузерное хранилище, Git, отчёты или логи.
6. Для ограниченного OAuth нужен зарегистрированный точный HTTPS Redirect URI серверного callback. **Callback CRM пока не реализован и в банке не зарегистрирован**; исходное `https://` в ЛК непригодно. Back URL для «Компаниям» не нужен. Требуется сохранить state/nonce на сервере, проверить одноразовый callback, обменять код на токены через mTLS и сверить ИНН/счета. Минимальный банковский scope методов выписки, деталей и PDF — `GET_STATEMENT_ACCOUNT`; `inn orgFullName accounts` нужны для проверки организации. Не копировать полный scope из примеров Сбера.
7. По документации отдельное согласие на передачу данных третьему лицу не требуется при работе только со своей организацией. Для сторонних/дочерних организаций правила отличаются. Проверять счета и фактические права токена при каждой отдельной первичной авторизации.
8. Проверить исходящий TLS/mTLS на 9443 **из production**. Node должен доверять официальной цепочке и поддерживать криптографию контейнера. Для периодической сверки входящий mTLS/ГОСТ-шлюз вебхуков не требуется; вебхуки Сбера здесь не активированы.
9. Получить небольшой период (1–2 дня), сверить с банковской выпиской ИНН/счёт, поступления/списания, суммы/валюты, обе стороны, назначение и PDF. Повторить загрузку без дублей; при ошибке проверить сохранность предыдущих записей. Эти проверки на реальном API ещё не выполнены.
10. После сверки включить `ARTEL_BANK_SYNC_ENABLED=true`, подтвердить выполнение `.github/workflows/banking.yml` и изменение времени синхронизации после закрытия браузера. Наличие workflow и успешная сборка этого не доказывают. **Автоматическая банковская синхронизация пока не включена**.
11. Client secret в ЛК действует до 24.10.2026 (40 дней). Нужна ротация до истечения; автоматическая ротация не реализована. Документация также содержит получение бессрочного client_secret, но его условия и доступность для этого сервиса не проверены, метод не вызывался. OAuth access token действует 60 минут, refresh token — 180 дней с ротацией. Токены, полученные непосредственно в ЛК, имеют другие сроки и требуют проверки scope.

Участие владельца при повторении: контакты и ответственное лицо, подпись заявления, пароль контейнера и возможное СМС при авторизации/выпуске ключей. Пароли и СМС вводятся непосредственно в банковском интерфейсе; передача серверных секретов выполняется через защищённые настройки, не через чат.

### Точка передачи в новый чат после выпуска сертификата

- Пользователь запросил готовый запрос для продолжения в новом чате. Работа остановлена на передаче контекста, а не на завершённой интеграции. Реальные счета к CRM ещё не подключены, API-операций получено 0, работа синхронизации после закрытия браузера не подтверждена.
- Изменения этого этапа **локальные, не закоммичены и не опубликованы**: `server/banking/transport.ts`, `server/banking/routes.ts`, `server/local-api.ts`, `tests/api-banking.test.ts`, новый `server/banking/diagnostics.ts` и этот файл. Исходный HEAD — `2fa94a40a38ae32fb52f5ca2411a90a1b720355e`. Перед продолжением проверить текущий diff и изменения пользователя; не сбрасывать их.
- На этих серверных изменениях прошли 12 банковских тестов, typecheck, lint и build. Полный набор тестов после них не прогонялся. Тесты были синтетическими; контейнер пользователя и промышленный mTLS ими не проверены. `docs/banking.md` ещё нужно синхронизировать с поддержкой PFX и диагностикой.
- Ближайшие действия: безопасно определить расположение выпущенного `.p12`; установить клиентский сертификат и официальную цепочку доверия в серверные настройки; реализовать и зарегистрировать callback; получить ограниченные права чтения; проверить ИНН и полный номер счёта; опубликовать банковские изменения и выполнить проверку сети из Vercel. Очерёдность регистрации callback и авторизации должна учитывать доступность опубликованного endpoint.
- При реализации callback учесть текущие `SameSite=Strict` у сессионной cookie и запрет cross-site в серверной авторизации. Нужен отдельный безопасный поток с одноразовым state и проверкой организации; нельзя ослаблять общую авторизацию CRM ради банковского редиректа. Поддержку PKCE и дополнительных параметров нужно сверить с конкретной спецификацией Сбера, а не предполагать.
- Vercel CLI уже авторизован; при отсутствии команды в PATH использовался `node /Users/mustafa/.npm/_npx/69f9afb961c37556/node_modules/vercel/dist/index.js`. Защищённый результат env pull находится в `.vercel/sber-private/production.env`; некоторые чувствительные значения в нём заменены Vercel на `[SENSITIVE]`. Это не реальные значения, их нельзя загружать обратно вместо действующих production-секретов. Содержимое env-файлов не выводить.

### Официальные источники, проверены 14.09.2026

- [Наборы](https://developers.sber.ru/docs/ru/sber-api/start/overview), [личный кабинет и действия](https://developers.sber.ru/docs/ru/sber-api/start/connect), [промышленная интеграция](https://developers.sber.ru/docs/ru/sber-api/start/prom-stand).
- [Авторизация и согласия](https://developers.sber.ru/docs/ru/sber-api/start/oauth), [получение кода](https://developers.sber.ru/docs/ru/sber-api/specifications/oauth/oauth-authorize-get), [обновление токенов](https://developers.sber.ru/docs/ru/sber-api/specifications/oauth/oauth-token-post), [TLS](https://developers.sber.ru/docs/ru/sber-api/start/tls).
- [Выписка](https://developers.sber.ru/docs/ru/sber-api/specifications/statement/transactions), [детали операции](https://developers.sber.ru/docs/ru/sber-api/specifications/statement/transactions-id), [PDF операции](https://developers.sber.ru/docs/ru/sber-api/specifications/statement/transaction-id-print), [сроки и HTTP 202](https://developers.sber.ru/docs/ru/sber-api/faq/overview).

### Продолжение с предоставленным PKCS#12 — 14.09.2026

- Файл существующего сертификата передан пользователем из Downloads. Пароль введён в локальную форму и подтверждён Node TLS; повторный выпуск не выполнялся. Секретные материалы сохранены только в `.vercel/sber-private/` с ограниченными правами, вне Git.
- Локальная проверка 12:32 UTC: TCP 9443=true, serverTlsVerified=true, clientCertificateConfigured=true, HTTP 401 без токена. Установлена официальная цепочка с `SberCA Root Ext`; архив `chain_prom.zip` сам по себе не покрывает фактический сертификат сервера. Результат не равен успешной авторизации.
- В защищённые production env добавлены client ID, PFX, пароль, CA, отдельный ключ шифрования, HTTPS callback и ожидаемый ИНН. Автосинхронизация явно выключена. До нового deployment эти настройки не доказывают работу production.
- Реализован отдельный OAuth callback с PKCE S256, одноразовым зашифрованным state, отдельной Lax cookie и привязкой к исходной сессии/роли CRM. Общая SameSite=Strict и запрет cross-site сохранены. Добавлены проверка claims/счетов/прав и использование сохранённых ротируемых токенов без первичного refresh token в env. Подробности и ограничение проверки ГОСТ-подписи указаны в docs/banking.md.
- Точное значение OAuth issuer, Client secret, полный разрешённый счёт и регистрация callback ещё требуют подтверждения. Без этих настроек авторизация закрыта. Факт копирования текста в форму сам по себе не подтверждает, что это Client secret.
- Добавлен диагностический ручной запуск workflow `banking.yml` с параметром `check=true`; выполняет только проверку транспорта из Vercel. Реальное выполнение отмечается отдельно ниже.
- 107/107 API-тестов, lint, build прошли. Резервная копия `production-before.json` перечитана, SHA-256 совпадает с ранее записанным. Существующие отгрузки, расчёты, распределения и данные других банков не менялись.
- По-прежнему 0 реальных API-операций в CRM, сверка банка/PDF и автосинхронизация после закрытия браузера не подтверждены.

### Проверка опубликованного транспорта — 14.09.2026, 12:44 UTC

- `b0eff07` опубликован через GitHub → Vercel; deployment `artel-q09wdmwnx-mustafas-projects-62eca26d.vercel.app` Ready. Из реального production вызван `/api/banking/dispatch?check=sber-network` с существующим CRON_SECRET: HTTP маршрута 200, region=iad1, TCP=true, serverTlsVerified=true, clientCertificateConfigured=true, ответ банка 401 без OAuth-токена, errorCode=null. Это подтверждает транспорт из production, не доступ к выписке.
- Client secret введён владельцем отдельно (он подтвердил, что это именно Client secret, а не пароль .p12), сохранён как Vercel Secret без раскрытия. Полный номер рублёвого счёта `…5571` сверен в банковском интерфейсе и внесён в серверный allowlist; полный номер не публикуется в Git.
- Точный callback `https://artel-crm.vercel.app/api/banking/oauth/sber/callback` сохранён в карточке банка и перечитан. Сессия директора/администратора production в Chrome доступна.
- Для первичной сверки issuer используется строгое ожидаемое значение промышленного Token Endpoint `https://fintech.sberbank.ru:9443` по аналогии с fintech issuer в официальной исторической спецификации. Это предварительное ожидание, не подтверждённый claim реального токена. Несовпадение будет отклонено; проверку нельзя отключать.
- Перед первым OAuth-запуском ключ шифрования приведён к сохранённой частной копии. Локальная форма теперь перечитывает актуальные настройки перед записью, чтобы давно открытая вкладка не затирала добавленные параллельно CA и конфигурацию. Реальных OAuth-токенов к этому моменту не было.
